query Comments($getCmtInput: GetCommentInput!) {
  comments(getCmtInput: $getCmtInput) {
    totalCount
    cursor
    hasMore
    paginatedComments {
      ...commentInfo
      numUsersLiked
      voteStatus
      user {
        id
        firstName
        lastName
        image_url
      }
    }
  }
}
