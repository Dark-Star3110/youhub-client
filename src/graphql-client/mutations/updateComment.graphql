mutation UpdateComment(
  $updateCommentInput: UpdateCommentInput!
  $commentId: String!
) {
  updateComment(
    updateCommentInput: $updateCommentInput
    commentId: $commentId
  ) {
    ...mutationStatuses
  }
}
